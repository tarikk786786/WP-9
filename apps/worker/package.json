{
  "name": "worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx src/keep.ts",
    "start": "tsx src/keep.ts",
    "worker": "tsx src/keep.ts",
    "once": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsx --test src/auth.test.ts src/session-policy.test.ts src/singleton.test.ts"
  },
  "dependencies": {
    "@bot/database": "*",
    "@bot/engine": "*",
    "@bot/shared": "*",
    "@whiskeysockets/baileys": "^6.7.21",
    "pino": "^10.3.1",
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "@types/node": "^24.12.0",
    "@types/qrcode": "^1.5.6",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
